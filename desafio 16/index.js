// Tabuada: Escreva um programa que gere a tabuada de multiplicação para um número específico.

const numeroTabuada = 8

for(let i = 1; i <= 10; i++) {
    console.log(`${i} x ${numeroTabuada} = ${i * numeroTabuada}`)
}

